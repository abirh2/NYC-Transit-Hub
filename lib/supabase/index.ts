/**
 * Supabase Client Exports
 * 
 * Browser client: import { createClient } from "@/lib/supabase/client"
 * Server client: import { createClient } from "@/lib/supabase/server"
 */

// Note: Don't re-export here to avoid accidentally importing server code in client components.
// Import directly from the specific file you need.

