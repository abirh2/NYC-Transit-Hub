# Deprecated compatibility notice

Project instructions are maintained in `AGENTS.md`, with focused references under `docs/ai/`.

Current Cursor versions discover the root `AGENTS.md` directly. Do not add project rules here; update the canonical files instead.
